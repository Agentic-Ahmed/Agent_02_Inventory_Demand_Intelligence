# Python
__pycache__/
*.pyc
read_docx.py
extract.py
draft_implementation_plan.md

# Contains MCP server secrets (Context7 API key)
.mcp.json

# Python virtualenv
.venv/

# Per-agent Gemini API keys and local DBs — never commit
backend/.env
.env
conversations.db
*.db
# SQLite WAL/SHM sidecar files
*.db-wal
*.db-shm
*.db-journal

# Eval checkpoints and run logs
backend/.eval_state/
*.log

# Downloaded datasets (re-downloadable from Kaggle; keep the repo lean)
backend/.data/

# Stray doc-extraction output
extracted_doc.txt

# Third-party skills installed into .claude/skills (large; not part of the product)
.claude/skills/

