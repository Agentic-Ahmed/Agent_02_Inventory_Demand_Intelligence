openai-agents[litellm]
pydantic
python-dotenv
truststore
fastapi
uvicorn[standard]
# Auth: Clerk session-token (JWT) verification — only needed once Clerk is switched on
pyjwt[crypto]
# Postgres driver (prod data layer; SQLite is used when DATABASE_URL is unset)
psycopg[binary]
# Observability: send Agents-SDK + FastAPI traces to Logfire (only active when
# LOGFIRE_TOKEN is set; see backend/observability/tracing.py)
logfire[fastapi]
# Vector DB for semantic memory / RAG (only active when QDRANT_URL is set;
# see backend/core/memory.py)
qdrant-client
